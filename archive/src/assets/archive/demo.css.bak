/* Archived copy of demo.css (moved on May 31, 2026) */

/* Custom scrollbar for the textarea and chat area */
::-webkit-scrollbar {
    width: 8px;
}
::-webkit-scrollbar-track {
    background: transparent;
}
::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 4px;
}
.dark ::-webkit-scrollbar-thumb {
    background: #475569;
}
::-webkit-scrollbar-thumb:hover {
    background: #94a3b8;
}
.dark ::-webkit-scrollbar-thumb:hover {
    background: #64748b;
}

/* Hide scrollbar for clean look unless scrolling */
textarea::-webkit-scrollbar {
    width: 0px;
}
textarea:focus::-webkit-scrollbar {
    width: 8px;
}

.dropdown-enter {
    opacity: 0;
    transform: scale(0.95) translateY(10px);
}
.dropdown-enter-active {
    opacity: 1;
    transform: scale(1) translateY(0);
    transition: opacity 200ms ease-out, transform 200ms ease-out;
}
.dropdown-exit {
    opacity: 1;
    transform: scale(1) translateY(0);
}
.dropdown-exit-active {
    opacity: 0;
    transform: scale(0.95) translateY(10px);
    transition: opacity 150ms ease-in, transform 150ms ease-in;
}

/* Welcome copy intro animation */
@keyframes revealUp {
    0% {
        opacity: 0;
        transform: translateY(18px) scale(0.985);
        filter: blur(4px);
    }
    100% {
        opacity: 1;
        transform: translateY(0) scale(1);
        filter: blur(0);
    }
}

/* (rest of original demo.css archived) */
